# DO NOT put real secrets in browser code.
# Create a new Airtable token because the prior one was exposed in chat.
AIRTABLE_TOKEN=replace_with_new_airtable_personal_access_token
AIRTABLE_BASE_ID=app0N8QuzDVd5jmfN
AIRTABLE_TABLE_NAME=Formulary

# Optional shared edit key.
# Leave blank to allow public edits, or set a value to require users to enter this key before saving changes.
EDIT_KEY=
